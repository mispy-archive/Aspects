namespace = aspects

character_event = {
  id = aspects.inherit_law
  desc = aspects.inherit_law.desc
  picture = GFX_evt_bishop

  trigger = {
    trait = heir_of_law
    NOT = {
      god_of_law {
        is_alive = yes
      }
    }
  }

  option = {
    name = OK
    remove_trait = heir_of_law
    add_trait = god_of_law
    k_tokhar {
      add_claim = ROOT
    }
  }
}

character_event = {
  id = aspects.inherit_secrets
  desc = aspects.inherit_secrets.desc
  picture = GFX_evt_bishop

  trigger = {
    trait = heir_of_secrets
    NOT = {
      god_of_secrets {
        is_alive = yes
      }
    }
  }

  option = {
    name = OK
    remove_trait = heir_of_secrets
    add_trait = god_of_secrets
    k_chald {
      add_claim = ROOT
    }
  }
}
